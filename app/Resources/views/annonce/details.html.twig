{% extends "base.html.twig" %}
{% block title %}détails{% endblock %}

{% block body %}
    <div class="row">
        {% for a in add %}
            <div class="col-md-12">
                <p>{{ a.description }}</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}